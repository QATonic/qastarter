# {{projectName}} - REST API Test Automation
# Multi-stage Dockerfile for running RestAssured API tests

FROM gradle:8.5-jdk17 AS build
WORKDIR /app

# Copy build files and download dependencies
COPY build.gradle settings.gradle ./
RUN gradle dependencies --no-daemon

# Copy source code
COPY src ./src

# Build the project
RUN gradle compileJava --no-daemon

# Test stage
FROM gradle:8.5-jdk17 AS test
WORKDIR /app

# Copy built application
COPY --from=build /app ./
COPY --from=build /root/.gradle /root/.gradle

# Set environment variables
ENV TEST_ENV=qa
ENV GRADLE_OPTS="-Xmx1024m"

# Create directories for test results
RUN mkdir -p /app/build/reports/tests /app/build/allure-results

# Default command runs all tests
ENTRYPOINT ["gradle", "test", "--no-daemon"]
CMD ["-Denv=qa"]
