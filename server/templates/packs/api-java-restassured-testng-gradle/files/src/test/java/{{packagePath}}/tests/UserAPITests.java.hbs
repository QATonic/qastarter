package {{packageName}}.tests;

import io.restassured.response.Response;
import org.testng.annotations.Test;
import {{packageName}}.core.BaseTest;
import {{packageName}}.models.User;

/**
 * Sample API Test
 * 
 * HOW TO USE THIS TEMPLATE:
 * 1. Update the API_ENDPOINT below with your API URL
 * 2. Update the test data to match your API requirements
 * 3. Run: mvn clean test
 * 
 * This is a simple positive API test to verify your framework setup.
 * Add more test cases as needed for your project.
 */
public class UserAPITests extends BaseTest {

    // ========== UPDATE THESE VALUES ==========
    private static final String API_ENDPOINT = "/YOUR_API_ENDPOINT_HERE";
    // ==========================================

    @Test(groups = {"api", "smoke"}, 
          description = "Verify API endpoint returns successful response")
    public void testGetEndpoint() {
        logStep("Testing API endpoint");
        
        // Step 1: Send GET request to API
        logAction("Sending GET request to: " + API_ENDPOINT);
        Response response = apiClient.get(API_ENDPOINT);
        
        // Step 2: Validate response status
        apiClient.getResponseValidator().validateStatusCode(response, 200);
        apiClient.getResponseValidator().validateContentType(response, "application/json");
        
        // Step 3: Verify response is received
        logVerification("API responded successfully");
        
        // Test passed! Your framework is working correctly.
    }
}
