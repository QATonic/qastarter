import { apiClient } from '../../core/ApiClient';
import { SchemaValidator } from '../../core/SchemaValidator';
import { userSchema } from '../../models/User';

// ========== UPDATE THESE VALUES ==========
// Replace with your actual API endpoint
const API_BASE_URL = 'YOUR_API_BASE_URL_HERE';
const USER_ID = 1; // Replace with a valid user ID from your API
// ==========================================

describe('Users API Tests', () => {
  
  /**
   * Verify API connection and basic GET request works correctly.
   * 
   * BEFORE RUNNING:
   * 1. Update API_BASE_URL in config/settings.ts
   * 2. Update USER_ID above with a valid user ID from your API
   * 3. Update schema validation in models/User.ts to match your API response
   */
  test('should get user by ID', async () => {
    // Step 1: Make GET request to users endpoint
    const response = await apiClient.get(`/users/${USER_ID}`);
    
    // Step 2: Verify response status
    expect(response.status).toBe(200);
    expect(response.body.id).toBe(USER_ID);
    
    // Step 3: Validate response schema (optional)
    SchemaValidator.validate(response.body, userSchema);
    
    // Test passed! Your {{framework}} framework is working correctly.
  });
});
