using NUnit.Framework;
using {{projectName}}.Tests.Pages;

namespace {{projectName}}.Tests;

// ========== UPDATE THESE VALUES ==========
// Replace with your actual test credentials
// ==========================================

[TestFixture]
public class LoginTests : BaseTest
{

private LoginPage _loginPage = null!;
private HomePage _homePage = null!;

[SetUp]
public void SetUpPages()
{
_loginPage = new LoginPage(Driver);
_homePage = new HomePage(Driver);
}

/// <summary>
    /// Verify login functionality works correctly.
    ///
    /// BEFORE RUNNING:
    /// 1. Update TestUsername and TestPassword above with valid credentials
    /// 2. Configure device capabilities in Config/CapabilitiesConfig.cs
    /// 3. Ensure Appium server is running
    /// 4. Update selectors in Tests/Pages/LoginPage.cs to match your app
    /// </summary>
[Test]
public void ShouldLoginWithValidCredentials()
{
// Get credentials from config
var username = EnvConfig.Instance.GetUsername("validUser");
var password = EnvConfig.Instance.GetPassword("validUser");

// Step 1: Perform login
_loginPage.Login(username, password);

// Step 2: Verify home page is displayed
var isHomeDisplayed = _homePage.IsHomePageDisplayed();
Assert.That(isHomeDisplayed, Is.True, "Home page should be displayed after login");

// Test passed! Your {{framework}} framework is working correctly.
}
}