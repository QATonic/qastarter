package {{javaPackage}}.core;

import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import {{javaPackage}}.utils.Log;

/**
* Base Test - Common setup for all mobile tests.
*/
public class BaseTest {

@BeforeMethod
public void setup() {
Log.info("========================================");
Log.info("Mobile Test Started");
Log.info("========================================");
String platform = {{javaPackage}}.config.ConfigurationReader.getProperty("mobile.platform", "android");
DriverManager.initializeDriver(platform);
}

@AfterMethod
public void teardown() {
DriverManager.terminateDriver();
Log.info("========================================");
Log.info("Mobile Test Completed");
Log.info("========================================");
}
}