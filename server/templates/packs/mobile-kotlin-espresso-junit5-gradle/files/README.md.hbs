# {{projectName}}

Kotlin Espresso Android UI testing project generated by [QAStarter](https://qastarter.com).

## 🚀 Quick Start

### Prerequisites
- Android Studio Arctic Fox or later
- Android SDK 34
- JDK 11+
- Kotlin {{toolVersions.kotlin}}

### Setup

1. Open project in Android Studio
2. Sync Gradle dependencies
3. Connect a device or start an emulator

### Run Tests

```bash
# Run all instrumentation tests
./gradlew connectedAndroidTest

# Run specific test class
./gradlew connectedAndroidTest -Pandroid.testInstrumentationRunnerArguments.class={{groupId}}.tests.LoginTests

# Generate test report
./gradlew createDebugCoverageReport
```

## 📁 Project Structure

```
{{projectName}}/
├── app/
│ ├── build.gradle.kts
│ └── src/
│ ├── main/
│ │ └── AndroidManifest.xml
│ └── androidTest/
│ └── kotlin/{{packagePath}}/
│ ├── screens/
│ │ ├── BaseScreen.kt
│ │ ├── LoginScreen.kt
│ │ └── HomeScreen.kt
│ ├── tests/
│ │ ├── LoginTests.kt
│ │ └── SmokeTests.kt
│ ├── utils/
│ │ ├── TestUtils.kt
│ │ └── WaitHelper.kt
│ └── runner/
│ └── CustomTestRunner.kt
├── build.gradle.kts
├── settings.gradle.kts
└── gradle.properties
```

## 🧪 Writing Tests

### Using Screen Objects

```kotlin
@Test
fun loginWithValidCredentials() {
LoginScreen()
.enterUsername("testuser")
.enterPassword("password123")
.clickLogin()

HomeScreen()
.verifyWelcomeMessage("Welcome, testuser!")
}
```

## 🏷️ Test Tags

- `@SmallTest` - Unit tests
- `@MediumTest` - Integration tests
- `@LargeTest` - End-to-end tests

{{#eq reportingTool "allure"}}
## 📊 Allure Reports

```bash
./gradlew allureReport
./gradlew allureServe
```
{{/eq}}

## License

MIT