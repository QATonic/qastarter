package {{groupId}}.runner

import android.app.Application
import android.content.Context
import androidx.test.runner.AndroidJUnitRunner

/**
* Custom test runner for {{projectName}}.
* Provides hooks for test setup and teardown.
*/
class CustomTestRunner : AndroidJUnitRunner() {

override fun newApplication(
cl: ClassLoader?,
className: String?,
context: Context?
): Application {
// You can customize the application class here for testing
return super.newApplication(cl, className, context)
}

override fun onStart() {
// Clear app data before tests if needed
// targetContext.deleteDatabase("app.db")
super.onStart()
}
}