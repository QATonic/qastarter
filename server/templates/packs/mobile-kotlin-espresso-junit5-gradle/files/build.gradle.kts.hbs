// Top-level build file for {{projectName}}
plugins {
id("com.android.application") version "{{toolVersions.androidGradlePlugin}}" apply false
id("org.jetbrains.kotlin.android") version "{{toolVersions.kotlin}}" apply false
}

tasks.register("clean", Delete::class) {
delete(rootProject.buildDir)
}