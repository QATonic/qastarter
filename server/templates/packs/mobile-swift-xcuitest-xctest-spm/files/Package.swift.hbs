// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "{{projectName}}",
    platforms: [
        .iOS(.v15)
    ],
    products: [
        .library(
            name: "{{projectName}}",
            targets: ["{{projectName}}"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "{{projectName}}",
            dependencies: []),
        .testTarget(
            name: "{{projectName}}UITests",
            dependencies: ["{{projectName}}"]),
    ]
)
