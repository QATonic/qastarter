/**
 * Shared utilities for k6 performance tests.
 * Generated by QAStarter.
 */

/**
 * Default base URL for all tests.
 * Override with: k6 run -e BASE_URL=https://staging.example.com scripts/load-test.js
 */
export const BASE_URL = __ENV.BASE_URL || '{{baseUrl}}';

/**
 * Standard request headers.
 */
export const defaultHeaders = {
  'Content-Type': 'application/json',
  Accept: 'application/json',
};

/**
 * Common thresholds for performance tests.
 */
export const standardThresholds = {
  http_req_duration: ['p(95)<500', 'p(99)<1500'],
  http_req_failed: ['rate<0.01'],
  http_reqs: ['rate>100'],
};
