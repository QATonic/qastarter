"""
Performance test configuration.
Generated by QAStarter.
"""

# Target URL (override with --host flag)
BASE_URL = "{{baseUrl}}"

# Performance thresholds
THRESHOLDS = {
    # Maximum acceptable p95 response time in seconds
    "response_time_p95": 0.5,
    # Maximum acceptable p99 response time in seconds
    "response_time_p99": 1.5,
    # Maximum acceptable error rate (0.01 = 1%)
    "error_rate": 0.01,
    # Minimum requests per second
    "min_rps": 100,
}

# Load profiles
PROFILES = {
    "load": {"users": 50, "spawn_rate": 10, "run_time": "5m"},
    "stress": {"users": 200, "spawn_rate": 20, "run_time": "10m"},
    "spike": {"users": 500, "spawn_rate": 100, "run_time": "5m"},
    "soak": {"users": 50, "spawn_rate": 10, "run_time": "30m"},
}
