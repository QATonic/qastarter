"""
Default Locust performance test file.
Run: locust -f locustfile.py --host={{baseUrl}}

Generated by QAStarter.
"""

from locust import HttpUser, task, between


class DefaultUser(HttpUser):
    """Simulates a typical user browsing the application."""

    wait_time = between(1, 3)

    @task(3)
    def homepage(self):
        """Visit the homepage — most common action."""
        self.client.get("/", name="Homepage")

    @task(1)
    def health_check(self):
        """Hit a lightweight endpoint."""
        self.client.get("/api/health", name="Health Check")

    def on_start(self):
        """Called when a simulated user starts. Use for login/setup."""
        pass
