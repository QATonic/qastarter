# {{projectName}}

A production-ready **{{#if (eq framework "selenium")}}Selenium WebDriver{{else}}Playwright{{/if}}** test automation framework using **{{#if (eq testRunner "testng")}}TestNG{{else}}JUnit5{{/if}}** and **{{#if (eq buildTool "maven")}}Maven{{else}}Gradle{{/if}}**.

## 🚀 Quick Start

```bash
# Run all tests
{{#if (eq buildTool "maven")}}
mvn clean test
{{else}}
gradle clean test
{{/if}}

# Run with specific browser
{{#if (eq buildTool "maven")}}
mvn test -Dbrowser=firefox
{{else}}
gradle test -Dbrowser=firefox
{{/if}}

# Run headless
{{#if (eq buildTool "maven")}}
mvn test -Dheadless=true
{{else}}
gradle test -Dheadless=true
{{/if}}

# Run specific environment
{{#if (eq buildTool "maven")}}
mvn test -Denv=qa
{{else}}
gradle test -Denv=qa
{{/if}}
```

## 📁 Project Structure

```
src/
├── main/java/{{javaPackage}}/
│   ├── config/           # Configuration management
│   ├── core/             # BaseTest, {{#if (eq framework "selenium")}}DriverManager{{else}}PlaywrightManager{{/if}}
│   ├── pages/            # Page Objects (LoginPage, etc.)
{{#if (eq testRunner "testng")}}
│   ├── listeners/        # TestNG listeners
{{/if}}
│   └── utils/            # Log, Screenshot, {{#if (eq reportingTool "extent-reports")}}ExtentManager{{/if}}{{#if (eq reportingTool "allure")}}AllureManager{{/if}}
├── main/resources/
│   └── config/           # dev.properties, qa.properties
└── test/
    ├── java/{{javaPackage}}/
{{#if (eq testingPattern "bdd")}}
    │   ├── runners/      # Cucumber TestRunner
    │   └── stepdefinitions/
{{else}}
    │   └── tests/        # Test classes
{{/if}}
    └── resources/
{{#if (eq testingPattern "bdd")}}
        └── features/     # Gherkin feature files
{{else}}
{{#if (eq testRunner "testng")}}
        └── testng.xml    # TestNG suite configuration
{{/if}}
{{/if}}
```

## ⚙️ Configuration

Edit `src/main/resources/config/dev.properties`:

```properties
base.url=https://www.saucedemo.com/
browser={{#if (eq framework "selenium")}}chrome{{else}}chromium{{/if}}
headless=false
username=standard_user
password=secret_sauce
timeout.default=30
```

## 🧪 Writing Tests

{{#if (eq testingPattern "bdd")}}
### Feature File (Gherkin)
```gherkin
@login
Feature: Login Functionality

  @smoke
  Scenario: Successful login
    Given I am on the login page
    When I login with valid credentials
    Then login should be successful
```

### Step Definitions
```java
@Given("I am on the login page")
public void iAmOnTheLoginPage() {
    // Implementation
}
```
{{else}}
### Page Object
```java
public class LoginPage extends BasePage {
    public void login(String username, String password) {
        enterUsername(username);
        enterPassword(password);
        clickLogin();
    }
}
```

### Test Class
```java
public class LoginTests extends BaseTest {
    @Test
    public void testValidLogin() {
        LoginPage loginPage = new LoginPage();
        loginPage.login("standard_user", "secret_sauce");
        Assert.assertTrue(loginPage.isLoginSuccessful());
    }
}
```
{{/if}}

## 📊 Reports

{{#if (eq reportingTool "extent-reports")}}
### ExtentReports
Reports generated in: `reports/ExtentReport_{timestamp}.html`

Open the HTML file in a browser after test execution.
{{/if}}
{{#if (eq reportingTool "allure")}}
### Allure Reports
```bash
# Generate and open report
{{#if (eq buildTool "maven")}}
mvn allure:serve
{{else}}
gradle allureServe
{{/if}}
```
Results in: `target/allure-results`
{{/if}}
{{#if (eq reportingTool "testng-reports")}}
### TestNG Reports
Reports generated in: `target/surefire-reports/`
{{/if}}

## 🖥️ Browsers

| Browser | Config Value |
|---------|--------------|
{{#if (eq framework "selenium")}}
| Chrome | `chrome` |
| Firefox | `firefox` |
| Edge | `edge` |
{{else}}
| Chromium | `chromium` |
| Firefox | `firefox` |
| WebKit | `webkit` |
{{/if}}

## 📸 Screenshots

Screenshots are automatically captured on test failure:
- Location: `screenshots/`
- Format: `FAILED_{TestName}_{timestamp}.png`

## 🔧 System Requirements

- Java 17+
- {{#if (eq buildTool "maven")}}Maven 3.9+{{else}}Gradle 8+{{/if}}
{{#if (eq framework "playwright")}}
- Playwright browsers (auto-installed on first run)
{{/if}}

---
Generated by [QAStarter](https://github.com/qatonic/qastarter)