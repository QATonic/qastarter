# {{projectName}}

Robot Framework web automation project generated by [QAStarter](https://qastarter.com).

## 🚀 Quick Start

### Prerequisites
- Python {{toolVersions.python}}+
- Chrome/Firefox browser
- pip

### Installation

```bash
# Create virtual environment
python -m venv venv
source venv/bin/activate # Linux/Mac
# or
.\venv\Scripts\activate # Windows

# Install dependencies
pip install -r requirements.txt

# Install browser drivers (for SeleniumLibrary)
rfbrowser init # For Browser library
```

### Run Tests

```bash
# Run all tests
robot tests/

# Run specific test suite
robot tests/login_tests.robot

# Run headless
robot --variable HEADLESS:true tests/
```

## 📁 Project Structure

```
{{projectName}}/
├── resources/
│ ├── common.resource # Common keywords and settings
│ └── pages/
│ ├── LoginPage.resource
│ └── HomePage.resource
├── libraries/
│ └── CustomLibrary.py # Custom Python keywords
├── variables/
│ ├── dev.yaml # Development variables
│ └── qa.yaml # QA variables
├── tests/
│ ├── login_tests.robot
│ └── smoke_tests.robot
├── results/ # Test results (generated)
├── requirements.txt
└── robot.yaml
```

## 🔧 Configuration

Edit `variables/dev.yaml` or `variables/qa.yaml` to configure:
- Base URL
- Credentials
- Timeouts

## 📊 Reports

After running tests, open `results/report.html` in a browser.

{{#eq reportingTool "allure"}}
### Allure Reports
```bash
allure serve results/
```
{{/eq}}

## 🏷️ Tags

- `smoke` - Quick sanity tests
- `regression` - Full regression suite
- `login` - Login functionality tests

## License

MIT