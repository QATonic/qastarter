# Robot Framework Requirements
robotframework>=6.1.0
robotframework-seleniumlibrary>=6.1.0
{{#if (eq reportingTool "allure")}}
allure-robotframework>=2.13.0
{{/if}}
webdriver-manager>=4.0.0

{{#if utilities.faker}}
# Test Data Generation
faker>={{toolVersions.faker}}
{{/if}}
{{#if (eq cloudDeviceFarm "browserstack")}}
# BrowserStack SDK — reads browserstack.yml at project root and routes
# every Selenium session to BrowserStack's cloud. Run tests via:
#   browserstack-sdk robot tests/
# after setting BROWSERSTACK_USERNAME + BROWSERSTACK_ACCESS_KEY env vars.
browserstack-sdk>=1.22.0
{{/if}}